<UserControl x:Class="KnowledgeBasev2.WPF.Views.CodePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
<<<<<<< HEAD
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dtos="clr-namespace:KnowledgeBasev2.Domain.DTOs;assembly=KnowledgeBasev2.Domain"
             xmlns:local="clr-namespace:KnowledgeBasev2.WPF.Views"
             xmlns:mdxaml="https://github.com/whistyun/MdXaml"
             xmlns:converter="clr-namespace:KnowledgeBasev2.WPF.Converter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/TextBlockStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.Resources>
            <converter:GuidLengthConverter x:Key="guidconverter"/>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="125"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- SEARCH GRID -->
        <Grid Grid.Column="1" Background="LightGray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ComboBox Height="30" VerticalAlignment="Center" ItemsSource="{Binding Categories}" Margin="5"/>
            <StackPanel Grid.Column="1" Background="Blue" Width="250" HorizontalAlignment="Left" Margin="10" VerticalAlignment="Center">
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Height="30" 
                         VerticalContentAlignment="Center" TextAlignment="Center" FontSize="14" FontWeight="SemiBold"/>
                <Button Content="Search" Height="25"/>
            </StackPanel>
        </Grid>
        
        <!-- ID SELECTION -->
        <ListView Grid.Row="1" ItemsSource="{Binding AllCodes}" Background="LightGray" ScrollViewer.VerticalScrollBarVisibility="Hidden"
                  SelectedItem="{Binding SelectedCode}" SelectedIndex="{Binding SelectedIndex, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type dtos:ReadUpdateDTO}">
                    <Border BorderThickness="1" CornerRadius="5" Background="SteelBlue" Padding="5 2" Margin="0" Width="100">
                        <TextBlock Text="{Binding Id, Converter={StaticResource guidconverter}}" Margin="2" HorizontalAlignment="Stretch" FontSize="10" Foreground="White"
                                   TextAlignment="Center"></TextBlock>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <!-- COMMAND INFO -->
        <Grid Grid.Row="1" Grid.Column="1" Margin="25 5">
            <Grid.Resources>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#dddddd"/>
                    <Setter Property="BorderBrush" Value="SteelBlue"/>
                    <Setter Property="BorderThickness" Value="1.5"/>
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="Padding" Value="5"/>
                </Style>
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" MaxHeight="500"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Grid.ColumnSpan="2">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Stretch">
                    <TextBlock Text="Code" Style="{StaticResource TextBlockLabel}"/>
                    <mdxaml:MarkdownScrollViewer Grid.Column="1" Markdown="{Binding CurrentDocument}" FontSize="25" Width="825"
                                          VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="0 5 0 0"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Grid.ColumnSpan="2">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="System" Style="{StaticResource TextBlockLabel}"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedCode.System}" Style="{StaticResource TextBlockInfo}" ></TextBlock>
                </StackPanel>
            </Border>

            <Border Grid.Row="2" Grid.ColumnSpan="2">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Technology" Style="{StaticResource TextBlockLabel}"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedCode.Tech}" Style="{StaticResource TextBlockInfo}" ></TextBlock>
                </StackPanel>
            </Border>

            <Border Grid.Row="3" Grid.ColumnSpan="2">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Language" Style="{StaticResource TextBlockLabel}"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedCode.Lang}" Style="{StaticResource TextBlockInfo}" ></TextBlock>
                </StackPanel>
            </Border>

            <Border Grid.Row="4" Grid.ColumnSpan="2">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Version" Style="{StaticResource TextBlockLabel}"/>
                    <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SelectedCode.Version}" Style="{StaticResource TextBlockInfo}" ></TextBlock>
                </StackPanel>
            </Border>

            <Border Grid.Row="5" Grid.ColumnSpan="2">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Description" Style="{StaticResource TextBlockLabel}"/>
                    <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding SelectedCode.Description}" Style="{StaticResource TextBlockInfo}" TextWrapping="Wrap"></TextBlock>
                </StackPanel>
            </Border>
        </Grid>
=======
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KnowledgeBasev2.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">CODE SNIPPETS</TextBlock>
>>>>>>> a52c645db36ba9ff1941710d4786694c0054c198
    </Grid>
</UserControl>
